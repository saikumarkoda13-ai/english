services:
  - type: web
    name: essay-scoring-app
    env: docker
    plan: free
    region: oregon 
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: essay-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
